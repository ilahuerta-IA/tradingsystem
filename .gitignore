# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
ENV/
env/
.venv/

# Environment Variables
.env
.env.local

# Credentials (NEVER commit real credentials)
config/credentials/mt5.json
config/credentials/tiingo.json
config/credentials/*.credentials
!config/credentials/mt5_template.json

# VS Code
.vscode/

# PyCharm
.idea/

# Jupyter
.ipynb_checkpoints/

# Generated images (heatmaps, equity curves, etc.)
images/

# Data Files (Large CSVs should not be in git)
data/*.csv
data/*.zip

# Logs and Reports
logs/
temp_reports/*.txt

# Operating System
.DS_Store
Thumbs.db
*.swp
*~

# Backtrader cache
*.bt

# Original/Legacy files (keep for reference but don't track changes)
# originals/

# Private AI context files (NEVER public — equivalent to credentials)
# Contains: axioms, strategy docs, live analysis, session history, bugs
# All private context lives in context/ folder. Root CONTEXT.md is just a pointer.
CONTEXT.md
context/

# Private VEGA settings (live demo configs with risk allocation)
config/settings_vega.py
# ORION GEX runtime artifacts (per-day CSV snapshots)
analysis/gex_*.csv
